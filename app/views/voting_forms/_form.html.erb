<%= form_for(voting_form) do |f| %>
  <% if voting_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(voting_form.errors.count, "error") %> prohibited this voting_form from being saved:</h2>

      <ul>
      <% voting_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
