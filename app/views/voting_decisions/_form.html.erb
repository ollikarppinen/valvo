<%= form_for(voting_decision) do |f| %>
  <% if voting_decision.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(voting_decision.errors.count, "error") %> prohibited this voting_decision from being saved:</h2>

      <ul>
      <% voting_decision.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :candidate %><br>
    <%= f.number_field :voting_form_count, value: 2 %>
  </div>
  <div class="field">
    <%= f.label :candidate_count %><br>
    <%= f.number_field :candidate_count, value: 2 %>
  </div>
  <div class="actions">
    <%= f.submit 'Create Vote', :class => 'btn btn-primary btn-lg' %>
  </div>
<% end %>
