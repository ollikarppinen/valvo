<div>
  <h1>Voting flow control</h1>
  <p>Start vote here:</p>
  <p><%= url_for(controller: 'voting_initiations', vote_id: @vote.id , host: request.host_with_port) %></p>
  <p>End vote here:</p>
  <p><%= url_for(controller: 'voting_conclusions', vote_id: @vote.id , host: request.host_with_port) %></p>
</div>
