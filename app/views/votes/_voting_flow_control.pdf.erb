<div>
  <h1>Voting flow control</h1>
  <p>You can start and end this vote from here:</p>
  <%= render partial: "qr_code", locals: { url:
      url_for(controller: 'votes', action: :show, id: @vote.id , host: request.host_with_port)} %>
  </p>
</div>
